import styled from 'styled-components';

import { size } from '../../layout/helpers';

export const Wrapper = styled.div`
  width: 100%;
  max-width: 900px;
  margin: 0 auto;
  padding: 0 ${size(4)};
`;
